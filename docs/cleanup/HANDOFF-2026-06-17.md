# Übergabe — Visual-Layer Konsolidierung (Stand 2026-06-17)

Für die Fortsetzung durch einen anderen Agenten (Codex / Antigravity). Self-contained.

## Wo wir stehen

- **Repo:** `mora-ui` (Ordner `E:\saimor\INTERFACE`), eigenes Git-Repo, Remote `Saimor-world/mora-ui`.
- **Branch:** `chore/cleanup-dead-code-sweep-2` (gepusht). Enthält alle Session-Commits. Basiert auf `feat/desktop-in-universe` (= main + 11 Commits; main ist Vorfahre).
- **PR-Empfehlung:** gegen `feat/desktop-in-universe` mergen (zeigt nur die Session-Commits), nicht gegen main.
- **Dev-Server:** läuft auf `:3000` (`npm run dev`, Ordner INTERFACE). Showcase aller Komponenten: `http://localhost:3000/tunnel`.
- **⚠ WICHTIG:** NIE `next build` laufen lassen, während der Dev-Server läuft — beide teilen `.next` und der Build schießt den Dev-Server ab. Für schnelle Checks `npx tsc --noEmit`. Vollen Build nur bei gestopptem Dev-Server.

## Erledigt heute

1. **Tunnel** (`aba4f5b`): 5 fehlende Visual-Komponenten ins `/tunnel`-Showcase (SemanticConstellation, OrbitalCanvas, Mycelium 2.5D/3D, MyceliumOverlay).
2. **Sweep 2** (`3440f9c`): 28 eindeutig tote Dateien + Duplikate gelöscht.
3. **Sweep 1** (`f3a6951`): 22 Legacy-Panes (`components/panes/*Pane.tsx`, abgelöst durch `apps/*`) + PaneShell gelöscht; 13 veraltete Tests weg, 2 Util-Tests auf `@/apps/work-session` umgebogen (grün).
4. **Produkt 1 „Der EINE Orb"** (`150e515` + uncommitted Politur): MoraOrb ist kanonisch (Glas-Hub umhüllt PlasmaOrb-Herz, beide 2D). LiquidOrb (3D/WebGL, tot, crashte Previews) gelöscht.
- Gesamt 67 tote Dateien / ~6.600 Zeilen weg. Jeder Schritt tsc+build grün.
- Vollständiges Inventar: `docs/cleanup/2026-06-17-dead-code-inventory.md`.

## ⚠ Orb — Struktur fertig, Ästhetik OFFEN

Die Konsolidierung (3 Orbs → 1) ist **fertig und korrekt**. Die **optische Politik** sitzt aber noch nicht — User: „fast schlechter" nach dem letzten Versuch.

- **Stand:** `components/mora/MoraOrb.tsx` (emerald-Farbe, zentriert, entgrauter Hub mit leuchtendem Rand, Glas-Glanz) — uncommitted, gleich committed. `PlasmaOrb.tsx` auf der sauberen Originalversion (mein Sphere-Lighting-Versuch wurde zurückgenommen, machte es matschig).
- **Was der User will:** „grundlegend besser, premium". Aktuell wirkt es wie ein generischer leuchtender Ball — kein echtes Volumen, Plasma teils ausgewaschen/marmoriert.
- **Empfehlung für nächsten Agenten:** Der Canvas-SimplexNoise-Ansatz in `PlasmaOrb` ist schwer auf „premium" zu tunen. Optionen: (a) Plasma ruhiger/cleaner (weniger Turbulenz, klarer Kern statt Marmor), (b) komplett anderer Render-Ansatz (sauberer Radial-Gradient-Glaskörper statt Per-Pixel-Noise), (c) eine echte Referenz vom User holen („soll aussehen wie X"). **Live iterieren** über `/tunnel` (Eintrag „Der Orb") — Screenshots vom User oder Browser-Tool. Klein/mittel nutzt den CSS-`useStaticOrb`-Pfad (`size<=20`), groß den Canvas.
- Zielbild laut User-Entscheidung: **„Balanced: Glas + Plasma-Herz"**.

## Roadmap — die restlichen Produkte (Reihenfolge = Empfehlung)

Prinzip immer: **erst aus allen Quellen das Beste minen → EIN Produkt bauen → dann Reste löschen.** Quellen siehe DEFER-Liste im Inventar.

2. **EIN Hintergrund (5→1)** — `StarField` + `ForestLightCanopy` + `OrganicBackground` + `AmbientDust` + `NeuralGrid` → ein benannter lebender Desktop-Hintergrund. Aktuell im Shell: `components/os/shell/MoraShell.tsx` (StarField + MyceliumOverlay + NeuralGrid gestapelt). Schnell, sichtbar, gleicher Stil-Hebel wie der Orb.
3. **EIN Mycelium (5→1)** — `MyceliumOverlay` (live, im Shell) + `MyceliumLayer` + `Mycelium25D` + `MyceliumField3D` + `MyceliumDropfield`. ZUERST mit User definieren WAS es ist (Vorschlag: Moras Gehirn / Wissensnetz wie Obsidian, gespeist aus echten Node-Daten), dann EINE Version, Rest löschen. User will es „mal sehen".
4. **EIN Mora-Raum (Chat)** — `ResonanceRoom` (= ist nur Chat) + `apps/chat` + `MemorySidebar` (das „Brain am Rand", behalten) + `MoraPulsePanel` + `MoraInsightPopup` → EINE Chat-Lösung. **GRÖSSTER Brocken, SCOPE-BEWUSST** (siehe Leitplanke).
5. **EIN Melde-System (5→1)** — `NotificationCenter` + `ActionTray` + `RadarCard` + dialogue/`ConfirmCard` + Toasts → ein Hinweis/Aktion/Bestätigung-System; „muss Sinn machen wann was kommt".

**Neu verorten (nicht mergen):**
- **Nightwatch** (`apps/nightwatch`) → Systemdienst, der den Mora-Server überwacht, auf dem das OS läuft (raus aus Dock-Logik).
- **Wall** (`components/panes/WallPane.tsx`, echte Security-Scan-Domains) → Vollbild-Fenster in der Demo/Low-Entry-Version, komplett neu gedacht.
- **Focus Mode** (`components/os/FocusMode.tsx`, aktuell Pomodoro/DND-Timer, nirgends sinnvoll verdrahtet) → Zweck geben + integrieren, oder streichen.
- **Finder** (Dateimanager, `apps/finder`) → modernisieren.

**Cleanup-Restschuld:**
- DEFER-Quellen erst nach Produktbau löschen (Inventar).
- Apps ohne eigene Tests (action-center, canvas, finder, grid, integrations, mail, tasks, team, terminal, timeline, users, website-dossier) → Tests gegen `apps/*` schreiben (Pane-Tests sind weg).

## Harte Leitplanken (nicht verletzen)

- **Playground-Scope:** „Playground" = `public_playground`-Modus (Demo + Wall + Besucher mit 20-Tage-Daten), erreichbar via Security-Check (`app/playground/page.tsx`); auch neu registrierte Accounts laufen da rein. **NICHT** die `MoraPlayground`-Komponente (nur in `MoraHubPane`). Jede Konsolidierung muss scope-bewusst bleiben: Besucher (`demo`/`tenant-public-playground`) darf keine Voll-User-Flächen sehen; Voll-User-Umbau darf `VisitorHomeSurface`/Playground nicht brechen. Gating: CoreLayer, HomeSurface, VisitorHomeSurface, Dock, MoraShell, navStore, useAuthBootstrapper.
- **Keine Fake-Systemwahrheit**, kein LLM-Score auf Truth/Scope/Importance, `evidence[]` Pflicht.
- **Nichts mergen/pushen/resetten/löschen ohne explizite User-Freigabe.** Vor jedem Löschen: `tsc --noEmit` als Wahrheits-Gate (hat heute eine falsch-tote Datei gefangen: `MoraContextLabel` wird von `apps/chat` genutzt).

## Verifikation
`npx tsc --noEmit` (prüft auch Tests) · Dev-Server stoppen, dann `npm run build` · jeweils eigener Branch + PR.
