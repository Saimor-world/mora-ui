# OS Practical Reset — scope

This branch is intentionally practical and visible.

- authenticated Home now uses `HomeSurfaceNext`
- Dock is reduced to orientation-level destinations
- a new visual migration CSS layer simplifies glass, window chrome and app surfaces
- `Capital` is a first-class OS app
- XRPL account reading is server-side and read-only
- no wallet secrets are stored or requested
- public/preview visitor surfaces remain unchanged
- Universe remains available as a signature exploration mode
