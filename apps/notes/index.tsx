'use client';
// Stub — replaced by full migration in Plan 2 (app-platform-migration-a)
import React from 'react';
import type { AppProps } from '@/lib/apps/types';
export default function NotesApp({ paneId }: AppProps) {
  return <div className="flex items-center justify-center h-full text-white/30 text-sm">Notizen wird geladen…</div>;
}
