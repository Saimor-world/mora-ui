'use client';
// Stub — replaced by full migration in Plan 3 (app-platform-migration-b)
import React from 'react';
import type { AppProps } from '@/lib/apps/types';
export default function TerminalApp({ paneId }: AppProps) {
  return <div className="flex items-center justify-center h-full text-white/30 text-sm">Terminal wird geladen…</div>;
}
