'use client';
// Stub — replaced by full Kanban implementation in Plan 4 (app-platform-new-apps)
import React from 'react';
import type { AppProps } from '@/lib/apps/types';
export default function TasksApp({ paneId }: AppProps) {
  return <div className="flex items-center justify-center h-full text-white/30 text-sm">Aufgaben-Board wird geladen…</div>;
}
