'use client';
// Stub — replaced by full Activity Feed implementation in Plan 4 (app-platform-new-apps)
import React from 'react';
import type { AppProps } from '@/lib/apps/types';
export default function TimelineApp({ paneId }: AppProps) {
  return <div className="flex items-center justify-center h-full text-white/30 text-sm">Zeitverlauf wird geladen…</div>;
}
