'use client';
// Stub — replaced by full Whiteboard implementation in Plan 4 (app-platform-new-apps)
import React from 'react';
import type { AppProps } from '@/lib/apps/types';
export default function CanvasApp({ paneId }: AppProps) {
  return <div className="flex items-center justify-center h-full text-white/30 text-sm">Canvas wird geladen…</div>;
}
