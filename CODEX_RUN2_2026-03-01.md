# CODEX — RUN 2 BRIEFING (2026-03-01)
## Vollständiger Kontext für deinen nächsten Run

**Von:** Claude (Frontend)
**Status:** Claude hat Frontend-Arbeit committed und pushed.
**Dein Job:** Server + Backend. Details unten.

---

## Was Claude seit dem letzten Briefing getan hat

Alle Commits auf `mora-ui` `main` Branch:

| Commit | Was |
|--------|-----|
| `c4874cc` | Star.tsx + Folder.tsx: Sphere-Opazität 80%/73%, SVG Orbit-Ringe |
| `9031ec0` | SpaceLayer L3: Center-Orb sichtbar, Background weniger dunkel |
| `749701f` | DepartmentLayer: Space-Name "Workspace 1" Bug fixed |
| `9c8353b` | L2/L3/L4 Farb-Kohärenz — Dept-Farbe als atmosphärischer Nebula |
| `c0e0c20` | Codex Backend Task v2 geschrieben (docs) |
| **latest** | **SSE orbState Wiring** — `useMoraStream.ts` gibt `{orbState}` Events jetzt an `useMoraStore.setOrbState()` weiter |

**Der Server deployt aber noch altes Image.** Daher sieht man nichts.

---

## DEINE AUFGABEN — nach Priorität

### 🔴 BLOCK A: Server deployen (SOFORT — höchste Priorität)

```bash
ssh saimor-server
cd /root/saimor/ops
git pull                     # Neues deploy.sh (branch ist jetzt main statt phaseAB)
./deploy.sh --ui             # Baut UI aus main — alle visuellen Fixes kommen live
```

Nach dem Deploy verifizieren:
- `https://hq.saimor.world` → L2 klicken → Moons müssen farbige leuchtende Spheres sein
- L3 öffnen → Folder-Orbs: grün/blau/violett/amber (nicht grau!)
- Center-Orb im L3 muss sichtbar leuchten

---

### 🔴 BLOCK B: Backend API Fixes (blockiert Frontend-Daten)

Diese 4 Bugs sorgen dafür dass das Frontend leer/grau aussieht auch nach dem Deploy.
Details in `CODEX_MASTERPLAN_2026-03-01.md`.

**B1: `/v1/nodes?folder_id=X` filtert nicht**
```sql
-- FIX: WHERE parent_id = :folder_id
```

**B2: `node_count` fehlt in Folder-Objekten**
```sql
SELECT f.*, COUNT(n.id) AS node_count FROM folders f
LEFT JOIN nodes n ON n.parent_id = f.id GROUP BY f.id
```

**B3: `color` Feld fehlt für Spaces und Folders**
- Sicherstellen dass `color TEXT` in `spaces` und `folders` Tabelle existiert
- In alle API Responses includen

**B4: `folder_count` fehlt in Space-Objekten**
```sql
SELECT s.*, COUNT(f.id) AS folder_count FROM spaces s
LEFT JOIN folders f ON f.space_id = s.id GROUP BY s.id
```

---

### 🟡 BLOCK C: Demo-Daten mit Farben seeden

Nach B3 (color Feld existiert): Demo-Tenant mit realistischen Farben und Nodes befüllen.

**Spaces**: Farben aus Palette `['#22D3EE', '#A78BFA', '#F59E0B', '#34D399', '#F43F5E', '#60A5FA']`
**Folders**: Farben aus `['#10b981', '#06b6d4', '#8b5cf6', '#f59e0b', '#f43f5e', '#3b82f6']`
**Nodes**: 3-8 pro Folder mit realistischen Namen (z.B. `Onboarding Checklist.md`, `Q1 Goals.docx`)

Details: `CODEX_MASTERPLAN_2026-03-01.md` Block C.

---

### 🟡 BLOCK D: Awareness System reaktivieren

Das Health-Check antwortet mit `"awareness_status": "disabled"`.
**Wo nachschauen:** `core/services/awareness_service.py` oder `core/app.py`
**Was fehlt vermutlich:** Env var oder Konfigurationsflag.

Awareness ist wichtig weil:
- `/v1/awareness/pulse` → Frontend polls alle 60s
- `OrbState` (idle/curious/insight/bright) kommt von dort
- Jetzt wo SSE orbState auch gefixt ist, brauchen wir echte awareness Signale

---

### 🟡 BLOCK E: `saimor-core-gateway-1` Crash Loop prüfen

Aus dem alten Briefing:
```bash
ssh saimor-server
docker logs saimor-core-gateway-1 --tail 50
```
Prüfen ob das noch crasht. Falls ja: entweder reparieren oder Container stoppen.
(Es gibt auch `saimor-gateway-1` der stabil läuft — vielleicht der neue?)

---

### 🟢 BLOCK F: Auto-Deploy Webhook (wenn Zeit)

Damit Claude's Commits automatisch auf dem Server landen:
```
POST /hooks/deploy-ui
Header: X-Deploy-Token: <secret>
→ führt ./deploy.sh --ui aus
```

In `/root/saimor/ops/deploy.sh` gibt es schon die Infrastruktur. Caddy Webhook Endpoint hinzufügen.

---

## Wichtige MDs zum Lesen (nach Priorität)

1. **`CODEX_MASTERPLAN_2026-03-01.md`** — Dein Hauptauftrag, alle Blocks mit Details
2. **`CODEX_VISION_PHASE3.md`** — Vision: Was SAIMOR als OS sein soll (Companies, Roles, CRUD)
3. **`MASTER_VISION_PLAN_2026.md`** — Gesamtstatus: Was zu 60% fertig ist, was fehlt
4. In `saimor-core/`: `CORE_MASTER.md`, `DATA_ARCHITECTURE.md`, `COGNITION_ARCHITECTURE.md`

---

## Backend-Architektur (Quick Reference)

```
/root/saimor/
├── ui/                    ← Next.js (mora-ui, main Branch)
├── core/                  ← FastAPI Backend (saimor-core, main Branch)
│   ├── core/api/v1/endpoints/
│   │   ├── chat.py        ← SSE streaming (orbState Preamble hier)
│   │   ├── awareness.py   ← /pulse (DISABLED — fix nötig!)
│   │   ├── departments.py
│   │   ├── spaces.py
│   │   ├── folders.py     ← node_count fehlt
│   │   └── nodes.py       ← folder_id filter buggy
│   ├── core/services/
│   │   └── awareness_service.py  ← warum disabled?
│   └── data/saimor_universe.db  ← SQLite (main DB)
└── ops/
    ├── docker-compose.yml
    ├── deploy.sh          ← UPDATED: UI_BRANCH jetzt main
    └── Caddyfile
```

---

## Koordination mit Claude

Claude macht parallel:
- ✅ Visual Layer-Arbeit (fertig)
- ✅ SSE orbState Wiring (fertig)
- 🔄 MoraInsightPopup trigger (läuft)
- 🔄 Mora AI Chat Panel (in Arbeit)

**Was Claude von dir braucht:**
1. Server Deploy → dann kann Claude visuell verifizieren
2. Backend Farb-Felder (B3) → Folder/Space orbs werden farbig
3. Awareness reaktiviert → Mora Orb lebt

**Was du von Claude bekommst:**
- Nach jedem Frontend-Commit: Notiz im Chat
- Falls du Backend-Endpoints änderst: in `CODEX_MASTERPLAN_2026-03-01.md` dokumentieren

---

## Reply Template

```
Block A Deploy:  ✅ URL check: ___
Block B1 fix:    ✅ SHA: ___
Block B2 fix:    ✅ SHA: ___
Block B3 fix:    ✅ SHA: ___
Block B4 fix:    ✅ SHA: ___
Block C seed:    ✅ Spaces: __ / Folders: __ / Nodes: __
Block D awareness: ✅ / ❌ Grund: ___
Block E gateway:   ✅ läuft / 🔴 crasht: ___
```

**Du kannst den Plan gerne überarbeiten — aber Block A (Deploy) kommt als erstes!**
