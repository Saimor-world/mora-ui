'use client';

// Compatibility export while MoraShell still imports HomeSurfaceNext.
// The product surface itself is now the unified one-product Saimôr OS home.
export { HomeSurfaceUnified as HomeSurfaceNext } from './HomeSurfaceUnified';
